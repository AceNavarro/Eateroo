@model IEnumerable<SubCategory>
@{
    ViewData["Title"] = "Sub Category List";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<br />
<div class="card shadow p-4">
    <div class="row">
        <div class="col-8">
            <h2 class="text-info m-0">Sub Category List</h2>
        </div>
        <div class="col-4 text-right">
            <partial name="_CreateButtonPartial" />
        </div>
    </div>
    <br />
    <div>
        @if (Model.Count() > 0)
        {
            <table class="table table-striped border">
                <tr class="table-secondary">
                    <th>@Html.DisplayNameFor(m => m.FirstOrDefault().Name)</th>
                    <th>@Html.DisplayNameFor(m => m.FirstOrDefault().CategoryId)</th>
                    <th></th>
                </tr>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@Html.DisplayFor(m => item.Name)</td>
                        <td>@Html.DisplayFor(m => item.Category.Name)</td>
                        <partial name="_TableButtonsPartial" model="item.Id" />
                    </tr>
                }
            </table>
        }
        else
        {
            <div class="alert alert-info">No sub category has been added yet.</div>
        }
    </div>
</div>

